import AppRoutes from '@/routes';

/**
 * App — root component. Routing + layout live in routes/index.jsx;
 * global providers are set up in main.jsx.
 */
function App() {
  return <AppRoutes />;
}

export default App;
